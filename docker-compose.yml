services:
  myapp:
    build:
      dockerfile: Dockerfile
      context: ./client
    container_name: myapp_frontend
    hostname: myapp_frontend
    env_file:
      - ./client/.env
    ports:
      - '4001:5173'
    restart: always

networks:
  default:
    name: nginx_tier
    external: true
